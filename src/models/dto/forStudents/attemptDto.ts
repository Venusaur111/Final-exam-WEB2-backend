export interface StudentAttemptResponseDto {
    id: string; // UUID[cite: 10]
    attemptOrderNumber: number; // Serial[cite: 10]
    score: number; //[cite: 10]
    submittedAt: string; //[cite: 10]
}